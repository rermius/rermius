/** @type {import('tailwindcss').Config} */
export default {
	content: ['./src/**/*.{html,js,svelte,ts}'],
	// Tailwind v4 uses CSS-based configuration via @theme in app.css
	// This config file is kept minimal for compatibility
	plugins: []
};
